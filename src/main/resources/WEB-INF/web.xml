<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Tanks Reverse Proxy</display-name>
    <description>Tanks Reverse Proxy REST API Application</description>

    <!-- Context parameters -->
    <context-param>
        <param-name>jakarta.ws.rs.Application</param-name>
        <param-value>TanksReverseProxy</param-value>
    </context-param>

    <!-- Default welcome file -->
    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Session configuration -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!-- CORS filter for cross-origin requests (optional) -->
    <filter>
        <filter-name>CorsFilter</filter-name>
        <filter-class>jakarta.servlet.Filter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>CorsFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

</web-app>